// import React from "react";
// const addRando = () => {
//   let indexRandom = Math.floor(
//     Math.random() * Object.keys(contactos).length + 4
//   );
//   const contactsArr = this.state.contactos;
//   contactsArr.push({ ...contacts[indexRandom] });
//   this.setState({ contactos: contactsArr });
// };

// export default addRando;
