import "./App.css";
import { useState } from "react/cjs/react.production.min";
import newContacts from "./contacts.json";

function App() {
  // const [contacts, setContacts] = useState([...newContacts.splice(0, 5)]);

  return (
    <div className="App">
      <>
        {/* <ul>
          {contacts.map((contact) => {
            return <li>{contact.name}</li>;
          })}
        </ul> */}
      </>
    </div>
  );
}

export default App;
