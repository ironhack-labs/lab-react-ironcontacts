import React, {useContext} from 'react';
import Button from 'react-bootstrap/Button';

const RandomButton = () => {

  return(
   <Button variant="primary">Add Random Contact</Button>
  );
}

export default RandomButton;