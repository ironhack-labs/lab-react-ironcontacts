import React, { Component } from 'react';

export default class SearchField extends Component {
  render() {
    return (
      <div>
        <input type="text" name="query" value={} onChange={} />
      </div>
    );
  }
}
