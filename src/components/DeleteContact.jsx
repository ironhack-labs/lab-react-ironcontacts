
// function DeleteContact(props) {

//     const deleteContact = contactId => {
//         const filteredContacts = props.contacts.filter(contact => {
//             return contact.id !== props.contactId;
//         });

//         props.handleDeleteContact(filteredContacts);
//     };
//     return (

//         <button onClick={deleteContact} className="btn-delete">Delete contact</button>

//     );
// }

// export default DeleteContact;