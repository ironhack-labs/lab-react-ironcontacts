import React from "react";

const Button = ({ newContact }) => {
  return (
    <button
      className="btn btn-dark filteredContacts"
      onClick={this.newContact}
    />
  );
};

export default Button;
