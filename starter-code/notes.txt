instalar AntDesign: npm i antd
para correr: npm start

En index js importar antd: import 'antd/dist/antd.css' (viene de ant)

importar en App.js: import contacts from './contacts.json'
APp.js : generar el state


otra forma de codear esto:
sortByPopularity = () => {
    this.setState(prevState => {
      const { contacts } = prevState;

      contacts.sort((a, b) => {
        if (a.popularity > b.popularity) return 1;
        else if (a.popularity < b.popularity) return -1;
        return 0
      });
      return { contacts };
    });
  };

  

  Aqui esta:

  sortByPopularity = () => {
    this.setState(prevState => {
      const { contacts } = prevState;

      contacts.sort((a, b) => a.popularity - b.popularity);
      return { contacts };
    });
  };

Es lo mismo estos 2:
  onCLick = () =¨> this.deleteOne(key)

  onClick = this.deleteOne